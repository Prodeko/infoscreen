const blueLight = '#0053a0';
const blueMedium = '#002e7d';
const blueDark = '#002851';
const highlightColor = 'tomato';
const textColor = '#4d4d4d';

const sidebarWidth = '30%';
const slideWidth = '100%';

const borderRadius = '2px';
const contentPadding = '20px';
const contentBackgroundColor = '#e6e6e6';
const contentBoxShadow = '2px 10px 33px 5px rgba(0, 0, 0, 0.2);';

const theme = {
  blueLight,
  blueMedium,
  blueDark,
  highlightColor,
  textColor,
  sidebarWidth,
  slideWidth,
  borderRadius,
  contentPadding,
  contentBackgroundColor,
  contentBoxShadow,
};

export default theme;
